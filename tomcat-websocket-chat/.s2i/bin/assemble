#!/bin/sh

echo "Calling existing assemble script"

/usr/local/s2i/assemble

echo `ls -al /deployments >/tmp/test.sout`

rm /deploymets/websocket-chat.war
cp -R /tmp/src/target/websocket-chat /deployments


echo "Removing .war file and copying exploded contents"
